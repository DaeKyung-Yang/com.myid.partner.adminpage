package iconloop.myid.partner.adminpage.domain.repository;

import iconloop.myid.partner.adminpage.domain.entity.OrgDid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrgDidRepository extends JpaRepository<OrgDid, String> {
}
